@AGENTS.md

## Git 同步規則（每次改動都要即時同步到 GitHub）

在這個專案工作時，遵守以下流程，確保每項改動都盡快進到 GitHub，而且不會覆蓋掉隊友：

1. **每完成一項改動就立刻提交**：先 `git add -A`，再 `git commit`，訊息用一句中文清楚描述改了什麼（例：「修正首頁主標題文字」）。不要累積一大堆改動才一次提交。
2. **推送前先拉取最新進度**：先 `git pull --rebase origin main`，再 `git push`。目前團隊共用同一個 GitHub 帳號、直接推 `main`，所以推之前務必先 pull，避免蓋掉對方剛推的內容。
3. **絕對不要 `git push --force`**（會覆蓋掉別人的東西）。
4. 若 `git pull --rebase` 出現衝突，先停下來把衝突解決、並向使用者說明清楚，不要硬推上去。
